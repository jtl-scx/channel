version: "3"
services:
  php-fpm:
    build: docker/php-fpm
    working_dir: /application
    volumes:
      - ./:/application/source
      - ./var:/application/var
  rabbitmq:
    image: rabbitmq:management-alpine
    ports:
      - "15672:15672"
      - "5672:5672"